--- a/krokiet/src/settings/model.rs
+++ b/krokiet/src/settings/model.rs
@@ -59,7 +59,7 @@ pub struct SettingsCustom {
     pub use_cache: bool,
     #[serde(default)]
     pub save_also_as_json: bool,
-    #[serde(default = "ttrue")]
+    #[serde(default)]
     pub move_deleted_files_to_trash: bool,
     #[serde(default)]
     pub ignore_other_file_systems: bool,
